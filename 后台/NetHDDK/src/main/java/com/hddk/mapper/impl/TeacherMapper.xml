<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hddk.mapper.TeacherMapper" >
    <select id="getTeacherByT_id" resultType="Teacher">
        SELECT * FROM teacher WHERE t_id=#{t_id}
    </select>
    <select id="getAllTeacher" resultType="">
        SELECT * FROM teacher
    </select>
</mapper>